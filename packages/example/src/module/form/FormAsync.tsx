import FormEvent from '@event-chat/antd-item';
import type { FC } from 'react';

const FormAsync: FC = () => (
  <FormEvent>
    <FormEvent.Item name="email">
      {/* <Input */}
      test
    </FormEvent.Item>
  </FormEvent>
);

export default FormAsync;
