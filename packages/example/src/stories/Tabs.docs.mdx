import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import Tabs from './Tabs';
import TabsStories, { Default } from './Tabs.stories';
import ListObserver from './components/ListObserver';
import TableList from './components/TableList';
import Tag from './components/Tag';

<Meta component={Tabs} of={TabsStories} />

# 示例：`Tabs` 标签页

## 介绍

`event-chat` 的一个 `Tabs` 演示案例，依赖 `@event-chat/core`

## 目录

<ListObserver />

## 基础使用示例

<Canvas
  additionalActions={[
    {
      title: 'code in GitHub',
      onClick: () => {
        window.open(
          'https://github.com/cgfeel/event-chat/blob/main/packages/example/src/components/Tabs.tsx',
          '_blank'
        );
      },
    },
  ]}
  of={Default}
  sourceState="none"
/>

## API 参数

<Controls />

### 包含事件

回调函数的参数类型如下：

<TableList
  data={[
    {
      desc: '切换标签时，用于标签项接收事件',
      keyname: 'tabItem',
      type: ['tab-item'],
    },
    {
      desc: '手动切换标签时，用于容器接收事件',
      keyname: 'tabsContainer',
      type: ['tabs-container'],
    },
    {
      desc: '用于切换标签，触发动画',
      keyname: 'tabsPick',
      type: ['tabs-pick'],
    },
  ]}
/>

<style>
  {`
  .category-item > a:first-child {
    float: none;
    left: -18px;
    margin-left: 0;
    position: absolute;
    transform: translateY(-50%);
    top: 50%;
  }
  .docs-story, .demo-wrap {
    background-color: #020917;
  }
  .docs-story > div:first-child {
    padding: 0;
    padding-bottom: 22px;
  }
  .floatingList ul {
    margin: 0;
  }
`}
</style>
